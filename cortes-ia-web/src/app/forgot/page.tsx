'use client';import {useState} from 'react';import {api} from '@/lib/api';export default function Page(){const [message,setMessage]=useState('');return <main className="auth"><h1>Recuperar senha</h1><form onSubmit={async e=>{e.preventDefault();const email=new FormData(e.currentTarget).get('email');try{await api('/auth/forgot','POST',{email});setMessage('Se houver uma conta, você receberá as instruções.');}catch{setMessage('Tente novamente mais tarde.');}}}><label>E-mail<input name="email" type="email" required/></label><button>Enviar instruções</button></form><p role="status">{message}</p></main>;}
